{
    "name": "EtsySentry",
    "install": "bash .cursor/install.sh",
    "start": "bash .cursor/start.sh",
    "terminals": [
        {
            "name": "server",
            "command": "DATABASE_HOST=127.0.0.1 DATABASE_PORT=5435 DISABLE_SERVER_JOB_RUNNER=true bun --cwd apps/server --env-file=\"$PWD/.env\" --watch src/index.ts"
        },
        {
            "name": "website",
            "command": "bun run --cwd apps/website dev --strictPort --port 3100"
        }
    ],
    "ports": [3100, 8080]
}
