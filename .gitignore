node_modules/
**/node_modules/
**/dist/
.context/
.DS_Store

.env
.env.*

# Varlock schema is the committed contract
!.env.schema

# Cursor-discovered fleet skills, seeded by .cursor/start.sh in cloud VMs
.agents/

# Claude-managed worktrees carry nested biome configs that break lint at config load
.claude/
